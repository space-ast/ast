# HPOP

参考 simulink 的仿真思想，将 HPOP 设计为一个个函数块/功能块(Function Block)的组合
在该思想指导下，实现了一个简易的仿真引擎
然后在此基础上，实现了航天相关的仿真模型

优点：
1. 可以将复杂的系统分解为多个函数块，每个函数块负责一个特定的功能，方便维护和调试
2. 可以通过共享输入，实现函数块之间的数据共享，减少某些参数的重复计算
3. 可以灵活组合函数块，形成不同的仿真模型，满足不同的需求

缺点：
1. 架构复杂，需要考虑函数块之间的依赖关系和数据共享等问题
2. 数据流复杂，需要建立函数块之间的连接关系，避免数据交互错误
